{% set name = "xmscore" %}
{% set version = "1.0.35" %}
{% set sha256 = "4c280d0529dbecfbbc1159c19f8fbd432946824ef80c0b33f4623bd26d7c9b6e" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  fn: {{ name }}-{{ version }}.tar.gz
  url: https://github.com/Aquaveo/{{ name|lower }}/archive/{{ version }}.tar.gz
  sha256: {{ sha256 }}

build:
  number: 0

requirements:
  build:
    - {{ compiler('cxx') }}
    - python =3.6
    - cmake >=3.0.0
    - boost =1.65.1
    - cxxtest >=4.0.0
    - pybind11 >=2.2.2
  run:
    - python =3.6
    - numpy

test:
  source_files:
    # - build/runner{% if win %}.exe{% endif %}
    # - build/xmscore_py.*
    - xmscore/python/*.py
    - xmscore/python/**/*.py
  commands:
    # - {% if unix %}build/runner{% else %}build\runner{% endif %}
    # - cd build
    - python -m unittest discover -v -p *_pyt.py -s ./xmscore/python
  import:
    - xmscore_py


about:
  home: aquaveo.com
  license: XMSNG
  license_file: LICENSE
  summary: 'Support library for XMS libraries and products.'
  doc_url: 'https://aquaveo.github.io/xmscore/'
  dev_url: 'https://github.com/Aquaveo/xmscore'

extra:
  recipe-maintainers:
    - swainn
    - Tsubashi
    - gagelarsen
    - alemon-aquaveo

{% set name = "xmscore" %}
{% set version = "1.0.21" %}
{% set sha256 = "223c47dd49e4654fbaa4b80a03401bfb6ef670bf9ba49ec84b318ac22cc2b1d1" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  fn: {{ name }}-{{ version }}.tar.gz
  url: https://github.com/Aquaveo/{{ name|lower }}/archive/{{ version }}.tar.gz
  sha256: {{ sha256 }}

build:
  number: 0

requirements:
  build:
    - {{ compiler('cxx') }}
    - python >3.0.0
    - cmake >=3.0.0
    - boost =1.65.1
    - cxxtest >=4.0.0
    - pybind11 >=2.2.2


test:
  source_files:
    - build/runner{% if win %}.exe{% endif %}
    - build/xmscore_py.cpython*
    - xmscore/python/*.py
    - xmscore/python/**/*.py
  commands:
    - {% if unix %}build/runner{% else %}build\runner{% endif %}
    - cd build
    - python -m unittest discover -v -p *_pyt.py -s ../xmscore/python
  import:
    - xmscore_py


about:
  home: aquaveo.com
  license: XMSNG
  license_file: LICENSE
  summary: 'Support library for XMS libraries and products.'
  doc_url: 'https://aquaveo.github.io/xmscore/'
  dev_url: 'https://github.com/Aquaveo/xmscore'

extra:
  recipe-maintainers:
    - swainn
    - Tsubashi
    - gagelarsen
    - alemon-aquaveo
